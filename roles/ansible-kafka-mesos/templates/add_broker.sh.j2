#!/bin/bash
./kafka-mesos.sh broker add $1 --cpus 1 --mem 1500 --heap 1200 --options file:kafka-mesos.properties --constraints node_type=like:slave_broker,hostname=unique --api=http://master.mesos:7000