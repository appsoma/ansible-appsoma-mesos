# Backend of the app $app_name
backend srvs_$app_name
{% if haproxy_use_ssl is defined and haproxy_use_ssl %}
   redirect scheme https if !{ ssl_fc }
{% endif %}
   mode http
   balance leastconn
   $servers

backend srvs_internal_$app_name
   mode http
   balance leastconn
   $servers
